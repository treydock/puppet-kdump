HOSTS:
  debian9:
    roles:
      - client
      - default
    platform: debian-9-amd64
    box: debian/stretch64
    hypervisor: vagrant
    vagrant_memsize: 1024
CONFIG:
  log_level: debug
  type: foss
